#ifndef JSEQUENCE_HPP
# define JSEQUENCE_HPP

#include "types.hpp"

t_vector_sizeT	generate_jSequence(const size_t &len);

// deque version
t_deque_sizeT	generate_jSequence_deque(const size_t &len);

#endif
